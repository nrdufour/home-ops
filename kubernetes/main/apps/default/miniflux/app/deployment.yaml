---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: miniflux
  namespace: default
  labels:
    app: miniflux
spec:
  replicas: 1
  selector:
    matchLabels:
      app: miniflux
  template:
    metadata:
      labels:
        app: miniflux
    spec:
      containers:
      - name: miniflux
        image: ghcr.io/miniflux/miniflux:2.0.49@sha256:8fed9d8cd79b7d5e3c008a54a5c1f48523199b9fccf634ca6bdf075bb12e182c
        envFrom:
          - secretRef:
              name: miniflux-secret
        env:
          - name: "BASE_URL"
            value: "https://miniflux.home"
          - name: "CREATE_ADMIN"
            value: "1"
          - name: "DATABASE_URL"
            valueFrom:
              secretKeyRef:
                name: miniflux-pguser
                key: uri
          - name: "DEBUG"
            value: "1"
          - name: "LOG_DATE_TIME"
            value: "15"
          - name: "POLLING_SCHEDULER"
            value: "entry_frequency"
          - name: "POLLING_FREQUENCY"
            value: "15"
          - name: "RUN_MIGRATIONS"
            value: "1"
        ports:
          - containerPort: 8080
            name: http-miniflux
        resources:
          requests:
            cpu: 12m
            memory: 64M
          limits:
            memory: 256M
        livenessProbe:
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 1
          failureThreshold: 3
          httpGet:
            path: /healthcheck
            port: 8080
