use_nix

# Set TMPDIR for darwin compatibility
export TMPDIR=/tmp

# Kubernetes
export KUBECONFIG="$(expand_path ./kubernetes/main/kubeconfig)"
export SOPS_AGE_KEY_FILE="$HOME/.config/sops/age/keys.txt"