apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wikijs-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: ca-cluster-issuer
    cert-manager.io/common-name: wikijs.home
    nginx.ingress.kubernetes.io/whitelist-source-range: 192.168.0.0/24
spec:
  tls:
    - hosts:
        - wikijs.home
      secretName: wikijs-ing-cert
  ingressClassName: nginx
  rules:
  - host: wikijs.home
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wikijs
            port:
              number: 3000
