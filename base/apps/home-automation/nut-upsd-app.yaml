apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nut-upsd
  namespace: argocd
spec:
  project: default
  sources:
    - path: base/apps/home-automation/nut-upsd
      repoURL: https://forge.internal/nemo/home-ops.git
      targetRevision: HEAD
      ref: values
    - repoURL: oci://ghcr.io/bjw-s/helm/app-template
      targetRevision: 3.7.3
      path: .
      helm:
        releaseName: nut-upsd
        valueFiles:
          - $values/base/apps/home-automation/nut-upsd/helm-values.yaml
  destination:
    namespace: home-automation
    name: 'in-cluster'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true