apiVersion: v1
kind: ConfigMap
metadata:
  name: marmithon-config
  namespace: irc
data:
  marmithon.conf: |
    server = "irc.nlnog.net:6667"
    nick = "Marmithon"
    ssl = false
    channels = ["#souk"]

