---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: toot-pg-backup-pvc
  namespace: mastodon
spec:
  storageClassName: nfs
  volumeName: pg-backup-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
