apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The plugin must be installed in the same namespace as the CNPG operator
# (typically "cnpg-system")
# Ref: https://cloudnative-pg.io/plugin-barman-cloud/docs/0.7.0/installation/
namespace: cnpg-system

resources:
  - https://github.com/cloudnative-pg/plugin-barman-cloud/releases/download/v0.7.0/manifest.yaml
