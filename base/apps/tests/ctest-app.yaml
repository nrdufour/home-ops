apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ctest
  namespace: argocd
spec:
  project: default
  source:
    path: base/apps/tests/ctest
    repoURL: 'https://forge.internal/nemo/home-ops.git'
    targetRevision: HEAD
  destination:
    namespace: tests
    name: 'in-cluster'
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true