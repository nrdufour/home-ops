name: ci
on:
  schedule:
    - cron: '0 */3 * * *'
  push:
    branches: ["main"]
    paths-ignore:
      - '!.forgejo/workflows/renovate.yaml'

jobs:
  renovate:
    name: Renovate
    runs-on: docker
    container:
      image: ghcr.io/renovatebot/renovate:39.72.4@sha256:ddcaec18fe9aaaddbac331842e498febf3c4cf1543113bca8ffb4bea0f29c8d9
      volumes:
        - /etc/ssl/certs/ca-bundle.crt:/etc/ssl/certs/ca-bundle.crt
    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
        env:
          NODE_EXTRA_CA_CERTS: /etc/ssl/certs/ca-bundle.crt
        with:
          show-progress: false
          repository: nemo/renovate-config
          token: ${{ secrets.RENOVATE_TOKEN }}
          path: ./renovate-config
      - name: Renovate
        uses: ./renovate-config/.forgejo/actions/common-renovate
        with:
          token: ${{ secrets.RENOVATE_TOKEN }}
          github-token: ${{ secrets.GH_TOKEN }}
          args: nemo/home-ops
          log-level: debug
